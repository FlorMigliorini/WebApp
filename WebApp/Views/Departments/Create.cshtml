@model WebApp.Models.Department

@{
    ViewBag.Title = "Create";
}

<div class="row p-4 ">
    <h2 class="text-black-50">Create</h2>
</div>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4 class="text-primary">Department</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group badge-light">
            @Html.LabelFor(model => model.Department_Type, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10 badge-light">
                @Html.EditorFor(model => model.Department_Type, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Department_Type, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group badge-light">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-outline-primary border-bottom p-5s mt-2" />
            </div>
        </div>
    </div>
}

<div class="btn btn-outline-primary border-info">
    @Html.ActionLink("Back to List", "Index")
</div>
